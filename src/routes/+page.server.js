export async function load(/*{ parent }*/) {
  /*const { orders, customers } = await parent()
  console.log({orders}, {customers})
  for (let ord of orders) {
    // @ts-ignore
    ord.customer = customers.find((c) => c.id == ord.customer_id)
  }
  return { orders, customers }*/
}